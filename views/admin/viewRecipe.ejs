
<h1 class="my-4"><%= recipe.name %></h1>

<div class="card">
    <div class="card-header">
        <h4 class="mb-0">Recipe Details</h4>
    </div>
    <div class="card-body">
        <p><strong>Category:</strong> <%= recipe.category %></p>
        <p><strong>Created By:</strong> <%= recipe.createdBy %></p>
        <p><strong>Description:</strong> <%= recipe.description %></p>
        <h5>Ingredients:</h5>
        <p><%= recipe.ingredients %></p>
        <a href="/admin/recipes" class="btn btn-secondary">Back to Recipes</a>
    </div>
</div>
