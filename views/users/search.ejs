<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search Results</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Styling for the card component */
    .card {
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Soft shadow */
  border-radius: 10px; /* Rounding the corners */
  transition: transform 0.2s ease-in-out; /* Hover animation */
  overflow: hidden; /* Ensures image fits within rounded corners */
}

.card:hover {
  transform: scale(1.05); /* Scale effect on hover */
}

/* Card image styling */
.card-img-top, .card-img-bottom {
  width: 100%; /* Image should take full card width */
  height: 200px; /* Set fixed height */
  object-fit: cover; /* Ensure image maintains aspect ratio and covers the area */
  object-position: center; /* Centers the image */
  border-top-left-radius: 10px; /* Rounded corners for top image */
  border-top-right-radius: 10px; /* Rounded corners for top image */
}

/* Ensure bottom image is aligned with rounded corners */
.card-img-bottom {
  border-bottom-left-radius: 10px;
  border-bottom-right-radius: 10px;
}

/* Card title and text */
.card-title {
  font-size: 1.25rem;
  font-weight: bold;
  color: #333;
}

.card-text {
  font-size: 0.95rem;
  color: #666;
  line-height: 1.5;
}

/* Button styling */
.btn-primary {
  background-color: #007bff;
  border: none;
  transition: background-color 0.3s;
}

.btn-primary:hover {
  background-color: #0056b3;
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .card {
    width: 100%; /* Full width on smaller screens */
  }

  .card-img-top, .card-img-bottom {
    height: 150px; /* Reduce image height for smaller screens */
  }
}

/* Card layout */
.row {
  margin-top: 20px;
}
  </style>
</head>
<body>

  <div class="container">
    <h1 class="pb-4">Search Results</h1>

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-5 g-4">
      <% if(recipe != '') { %>
        <% recipe.forEach(function(recipe, index){ %>
          <div class="col">
            <!-- Card with top image -->
            <div class="card mb-3">
              <img src="/uploads/<%= recipe.image %>" class="card-img-top" alt="<%= recipe.name %>" loading="lazy">
              <div class="card-body">
                <h5 class="card-title"><%= recipe.name %></h5>
                <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
                <p class="card-text"><small class="text-muted">Last updated <%= recipe.updatedAt %> ago</small></p>
                <a href="/recipe/<%= recipe._id %>" class="btn btn-primary">View Recipe</a>
              </div>
            </div>

           
          </div>
        <% }) %>
      <% } else { %>
        <p>No items found.</p>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
